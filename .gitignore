# Zig
.zig-cache/
zig-cache/
zig-out/

# Python (test harness)
.venv-test/
__pycache__/
*.pyc

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Logs / scratch
*.log
*.tmp

# Claude Code / agent state.
#
# Everything Claude Code writes into a working tree is local: the
# project memory file, per-user settings, session state, and MCP
# server wiring. `.github/workflows/claude-review.yml` is the one
# Claude-related file that IS tracked -- it runs on GitHub's runner,
# not from this directory.
.claude/
.claude.json
CLAUDE.md
CLAUDE.local.md
.mcp.json
bench-smoke.json
node_modules/

# The native Cedar benchmark harness builds here. Cargo.lock IS tracked --
# the row it produces is a latency number, so which cedar-policy built it
# has to be recoverable.
bench/native/cedar/target/
